# 開発タスク・進捗(TODO)

## 運用ルール

- このファイルには**今やっている/次にやるタスクのみ**を残す
- 完了したタスクはこまめに削除する(チェックを付けて残さない)
- 完了の過程で得た知見は、消す前に置き場所へ移す。実装設計は `docs/Architecture.md`、
  仕様は `docs/GameDesign.md`、値付けと測り方は `docs/BalanceReport_v5.md`。
  **検証で繰り返し踏む落とし穴は Architecture.md 11章にまとめてある**
- **「実機で確認する」は §2 の1箇所へ集める。**機能ごとの節に散らすと、同じ「実機確認」が
  10箇所以上に増えて、何が残っているのか誰にも読めなくなる(実際にそうなった)

---

## 1. 人が触って確かめること(実機確認)

ヘッドレスではGUIのクリックもホバーも届かず、演出の見え方も詰められない
(Architecture.md 11章)。**ロジックはいずれもヘッドレステストで押さえてあり、
ここに残っているのは「人の目でしか判定できないもの」だけ**。

### 1クライアントで確認できるもの

| 対象 | 見るところ |
|---|---|
| 対局中の演出 | 揺れの強さ・反転の行き過ぎ・破壊の破片。静止画では詰められない |
| カード詳細のホバー | 対局画面とデッキ編集。`hovered` / `mouse_exited` の配線と、消えるまでの猶予(0.15秒) |
| 時計工房・砂時計図鑑 | 駒のホバーと押下、段が多いときのスクロール、絵を押しての反転、語を押しての用語ポップ |
| キーワード辞書の実演 | 図版を紙の色にしたため、辞書の暗い背景の上で「紙に刷った図版を貼ってある」と読めるか |
| CPU戦の思考レベル | モーダルの表示・選択・対局への反映(上級の補正7項目すべて) |
| リーサルパズル | 操作と、エンドレスの難易度の手応え(`PuzzleGenerator` の `MIN_BRANCHING` 等はまだ初期値の見積もり) |
| デイリーミッション | 受取(残高が動く)。日をまたいで課題が入れ替わること |
| ショップ | 購入(残高が動く) |
| 反転権 | ボタン押下・対象選択・演出 |
| 攻撃のドラッグ | 2026-09-15実装。自分の駒を掴んで相手の駒/相手のHP帯へ落とす。掴んだ時点で対象が光り予測が出ること、守護がいる間はHP帯へ落とせないこと、途中で放したときの選択の残り方。**指している相手で攻撃側の予測が切り替わること**(相手の駒→HP帯→離す、で数値が変わる。ドラッグ中にも enter / exit が届く前提で作ってあり、届かなければ `_can_drop_data` から切り替える形へ直す) |
| ソロモードの10ステージ | 2026-09-11、上級CPU同士(プレイヤー役も上級CPUで代替)の自己対戦で各ステージ300戦ずつ実測した。#5「先を越された盤」(相手の初期展開3体→1体・fresh)・#8「二枚続き」(相手のswarm→shieldへ差し替え・HP30へ)・#10「砂時計の底」(相手の初期展開2体→1体・軽め)を調整し、いずれも勝率28〜30%へ収めた(#2 54% / #4 40% / #6 36% / #7 55% / #9 51%は変更なしで許容範囲)。**この測定は上級CPUの代替プレイであり、人間の実際の手応えとはズレうる**ため、パズル2問(テストで解けることは確認済み)とあわせて人が触って感触を見る |
| デッキ表の画像コピー | unityroomで `navigator.clipboard.write()` が通るか。断られたら右クリックコピーへ落ちるか。Xの投稿欄へ貼れるか |
| unityroomランキング連携(2026-09-15実装) | unityroomへ実際に書き出して公開した状態で、プラチナに到達したときスコア送信が成功すること(unityroom側のゲーム管理画面でそのボードの反応を見る)。**ボードを「常に記録」に設定していないと動作確認できない**ため、先にunityroom側の設定が要る |
| シーズン表彰式(`CardSeasonCeremonyPanel`、2026-09-15実装) | ポップインの尺・拡大の勢い、紋章の色・文言の見え方。**実際にシーズンをまたがないと呼ばれない**ため、`RankProgress.current_season_key()`の`at_unix_time`を差し替えたテスト用ビルド、またはシーズンが実際に切り替わるのを待って確認する |
| デッキコード | 発行したコードを別の端末で読み込めること |
| 掲示板〈ラボ〉(2026-09-17実装) | ホーム画面「つくる」タブ→掲示板画面が開くこと。投稿フォームで残高が減り一覧に並ぶこと(承認前は自分にも見えない想定なので、承認後に見える)。「Good」投票が1回だけ通り2回目は拒否されること。**`functions/lab_admin.js`のデプロイと`LAB_ADMIN_SECRET`の設定、`tools/lab_admin/index.html`からの承認・却下・採用確定が実際に反映されること**(§4.5にデプロイ手順を記載) |
| 砂時計の絵の1組化 | unityroomで読み込みが速くなっていること(pck 9.65MB → 3.13MB) |
| 日曜イベント | 日曜以外の日に日付判定だけ差し替えて動作確認 |
| カードセット「五砂の刻」(コンボ5枚) | ショップでの購入導線、デッキ編集での未購入時ロック表示、購入後の解放。総量5の条件が対局中に正しく判定されているか(硝子/被弾/相打ちで削れて5になった瞬間) |
| ランダムマッチ画面(専用画面化) | デッキ選択後にこの画面へ入ること、巡回ドット・戻るボタンでのキャンセル、募集通知の丸い印の位置(EmptyStateの中央寄せに対して自然に見えるか) |
| スマホ実機(iPhone Safari) | 2026-09-11、以下3点を直したが**未検証**。①(2026-09-16、下記の理由で無効化。今後は`MobileTextInput`の検証に差し替え)②長時間プレイしても自動リロードが起きにくくなったか(`HourglassArt`が焼き付け後に生画像(全カード×3状態、数十MB相当)を保持し続けていたのを解放。加えて`progressive_web_app/ensure_cross_origin_isolation_headers`をOFF化し、既定のHTMLシェルが機能検知に失敗した際`window.location.reload()`を呼ぶ経路自体を塞いだ)。③デッキ編集・図鑑等のカード一覧を指でスクロールできるか(既存の`ScrollContainer`任せのままで、今回は変更していない) |
| 文字入力欄のタップ(`MobileTextInput`、2026-09-16実装) | ユーザー実機で「ログインID/パスワード・ルームコード・デッキ名の欄をタップしても反応しない」と報告あり。調査の結果、GodotのHTML5書き出しは`html/experimental_virtual_keyboard`を有効にしても実機のモバイルブラウザで`LineEdit`の仮想キーボードが開かないエンジン側の既知の欠陥があると判明([godotengine/godot#39124](https://github.com/godotengine/godot/issues/39124)等)。Web書き出し+タッチ端末のときだけ`LineEdit`のタップを`window.prompt()`へ差し替える`MobileTextInput`を追加し、ログインID/パスワード/表示名/デッキ名/ルームコード/デッキコード/カード検索の7箇所へ配線した。**スマホ実機での動作未検証。**タップでOS標準の`window.prompt()`ダイアログが開き、文字が入力・反映されるか確認する。パスワード欄は`window.prompt()`の制約上マスキングできず平文表示になる点は仕様として許容済み(ユーザー承認済み) |
| 紋章の演出(打撃/恵与/払拭/反転) | 2026-09-14実装。ハンマー等の打撃、ワンド等の恵与(舞い降り)、ブランクの払拭(色抜け)、アイ/ピボットの反転(回転+`play_flip()`)、相手プレイヤーを狙う打撃(ソード等)、エコー/クラック等のドローの合図(紋章の光の輪)。型ごとの尺・色味・輪の見え方は未調整のまま実装しただけなので、実際に対局で見て手触りを詰める |
| 紋章の演出(出どころの拡張)| 2026-09-21実装。**砂術**(情報帯の上で紋章が浮き上がってから飛ぶ / 砂の補給・砂王の呼び声はその場で光の輪)、**余砂**(砕けた台座に銘板が残ってから飛ぶ。相打ちで死んだ場合は攻撃の着弾後)、**味方全体の恵与**(レガシー・アンカー・ドリップ・ラトルへ同時に舞い降りる)、**砂へ還す**(駒が縮んで手札へ吸われる)、**時の逆流**(自分のHPバーへ回転しながら飛ぶ)、**反転・着地の銘板の光**。尺・大きさは初期値のまま |
| 紋章の演出(全体打撃の同時多発)| 2026-09-14実装。スイープ・バースト・オベリスク・砂塵嵐・砂嵐で、相手の場にいる砂時計すべてへ紋章が同時に飛ぶ。`EmblemStrikeFx`を複数飛翔対応へ拡張し`effect_struck_many`を新設(ロジック側はヘッドレステストで確認済み)。6枠すべて埋まっている状態で撃ったときに紋章が重なって見づらくないか、実際の対局画面で確認する |

### 2クライアントが要るもの

| 対象 | 見るところ |
|---|---|
| ランダムマッチの成立 | 双方の画面でランダムマッチ画面から対局画面へ遷移すること、先手/後手が食い違わないこと |
| ルームマッチ画面 | 参加コードのコピー、持ち時間を切った対局で双方とも時計が出ないこと、版違いの弾き方 |
| 切断からの復帰 | **保険の機能であり、動かなくても対局は成立する**ため優先しない |
| 先手・後手のランダム化 | 何度か対局を成立させ、部屋を作った側・先に待っていた側が毎回同じ側にならないこと。**双方の画面で先手・後手が食い違わないこと**(食い違うと互いのデッキを待ち続けて始まらない) |
| 対局の記録と分析 | 1局通して `match_records` が1件だけ残り、`stats/global` が1増えること |
| 戦績のFirestore同期(2026-09-17実装) | 同じアカウントで別端末(または再インストール)からログインし、`きろく`タブの戦績に対局数・勝率が引き継がれていること。オフライン(CPU戦)で対局した直後にネットが無い状態から復帰したとき、戦績が失われず送り直されること |
| ランクマッチの成立(2026-09-15実装) | 双方の画面でランクマッチ画面から対局画面へ遷移すること、対局後に両者の段位(★またはレート)が動くこと、`CardRankScreen`のランキングに両者とも(ブロンズのままでも)並び、進行度の順で並んでいること |

### Discord側で確認するもの

- [ ] `/card` `/deck` `/link` `/profile` の4コマンドと、カードスポットライトの自動投稿。
      デプロイ・コマンド登録は完了済みで、**Discord上で実際に打つ作業だけが残っている**

---

## 1.5. 紋章の演出:今回やらなかった分(2026-09-14)

「紋章が対象へ飛んで働きかける」演出(1章の表の行を参照)は、**単体を狙う効果**
(`ENEMY_UNIT` / `ALLY_UNIT` / 相手プレイヤーそのもの)へ一括で適用した。以下は
提案では挙げたが、今回は意図的に対象外にした。1枚ずつの専用演出になるため、
まとめて実装する効率が働かず後回しにした。

- [x] ~~全体に効く効果(スイープ・バースト・オベリスク・砂塵嵐・砂嵐)~~ →
      2026-09-14実装。`EmblemStrikeFx`を複数同時再生できる形へ拡張し、
      `MatchState.effect_struck_many`(GameDesign.md 9章)で相手全体へ同時に紋章を飛ばす。
      味方全体を狙う恵与効果(レガシー・アンカー等)は対象外のまま(従来どおり光の筋)
- [x] ~~砂へ還す / 時の逆流 / 味方全体の恵与 / 砂術全般 / 余砂~~ → 2026-09-21実装
      (GameDesign.md 9章、Architecture.md 4.0節「紋章の出どころ」)。実機確認は§1へ
- [ ] **毒砂(Poison)**。`CardEffectResolver` を経由せず戦闘(`_resolve_unit_combat()`)
      の中で解決されるキーワードのため、今回の仕組みには乗っていない。対象が
      輪郭ごと溶けるように消える、という案があるなら別途検討する

## 2. ホーム画面のタブ再編(実装済み・実機確認が残り)

仕様は GameDesign.md 9章、実装設計は Architecture.md 4章「タブの再編」。
**対局の入口が4タブすべてに散っていた**状態を解いた。モックで形を詰めてから実装し、
4タブとも実際に描画して確認済み。

- [ ] **実機での確認。**タブの切り替え・選択中のせり出し・きろくタブの印・
      復帰の帯(戻れる対局があるときだけ出る)・マッチングの待機表示。
      とくに**復帰の帯は、切断した対局が残っている状態を作らないと出ない**ため、
      2クライアントでの検証(§1)と併せて見る
- [ ] **寸法・余白の詰め。**描画して直したのは「枠を領域の中央へ」「タブの幅を共通に」
      「残高を右端へ」「紋章の透かしに上限」「大きな札の見出しを上寄せ」の5点。
      実機で触ってさらに気になるところがあれば直す
- [ ] `RulesTab` の説明文(キャプション)の位置は枠の外に置いたままで、
      `HomeFrame` の行(`rows`)へ寄せていない。読めているので急がないが、
      枠の中へ収める形へ揃えられる

## 3. 対局画面の再構築(2026-09-21・着手中)

仕様は GameDesign.md 9章「対局画面の再構築」、設計は Architecture.md 10.10.0節。
モックは `scratchpad/audit/mock_A〜C.jpg`(C の卓 + B の照明を採用)。

- [x] 段階1: 下地(石の広間・吊りランプ・光だまり・落ち影)/ 台形の卓と濃紺のフェルト /
      真鍮の器の台座 / 行動の列のパネルと丸ボタン
- [x] 段階2: 情報帯の再スタイル(濃紺の地・真鍮の縁・丸い時計)/ 手札の面を情報帯の下へ /
      相手の列を0.92倍
- [ ] 吊りランプは情報帯の裏に隠れてほぼ見えない(隙間が10pxしか無い)。情報帯を詰めるか
      ランプを省くかを実機で見て決める
- [ ] 持ち時間の丸い時計はCPU戦では出ないため未確認(オンラインの実機確認で見る)
- [ ] 画面左の「直前の手」の列は旧来のまま(小さな板が縦に並ぶ)。再構築の意匠に合わせる
- [ ] 実機で光の強さ・床の明るさ・奥行きの度合いを詰める(§1へ)

以下は再構築より前に書いた改修案。段階1・2に吸収されるものは消す。

### (旧)対局画面のレイアウト改修(ブリーフ作成済み)

ホーム画面と同じ手当てを対局画面へ入れる。**仕様(GameDesign.md 9章)に書いてあるのに
実装が追いついていない点が4つ、見た目の不揃いが3つ**あり、そのまま着手できる7タスクへ
落としてある。

- 手渡し用のブリーフ: https://claude.ai/code/artifact/c7616148-97d6-45f0-b91d-1ff1b7a4f54b
- [ ] T1 卓の上下を同じ材で描く(マット「なし」のとき上下で面が違って見える)
- [ ] T2 「直前の手」を卓の左へ上から3〜4件積む(いまは1件が縦の中ほどに浮いている)
- [ ] T3 打点アシストを情報帯へ移す(行動の列でボタンに見えている)
- [ ] T4 空き枠の台座を数えられるようにする
- [ ] T5 手札を手元の面の中央へ揃える
- [ ] T6 行動の列を性格でまとめ、縦の間隔を2種類にする
- [ ] T7 情報帯の並びを詰め、上下で同じ位置に同じものを置く

**演出(攻撃の4段・反転の光の筋・破壊の崩落・砂の飛散)には触らない。**9章が細かく
定めており、この回で直すのは置き場所だけ。

---

## 3.5. 対局画面の手触り(2026-09-15・着手中)

仕様は GameDesign.md 9章「対局画面の手触り」、実装設計は Architecture.md 10.10.1節。

- [x] A 手札:隣が避ける / ドラッグの傾き / 放した位置から台座へ滑る / ドローのスライド / 相手手番で沈む
- [x] B マナと数字:ホバーでコストぶんのピップが光る / 支払いで吸われる / バッジの跳ね / 取り消しの戻り
- [x] C 対象選択と音:身構え / 攻撃ドラッグの矢印 / ホバー音
- [ ] 砂落ちの音(CC0素材が要る。ユーザーが探す)
- [ ] 実機で手触りを確認して尺・振れ幅を詰める(§1へ移す)

## 3.6. メニュー画面群の手触り(2026-09-15・着手中)

仕様は GameDesign.md 9章「メニュー画面群の手触り」、実装設計は Architecture.md 10.10.2節。

- [x] D 全画面共通:押し込みを深く / 遷移の横移動 / Escで戻る / タイトルの着地
- [x] E ホーム:タブの横滑り / 札のホバー浮き / 副題の変化の光り / 砂金の着地演出
- [x] F デッキ編集:一覧⇄棚の飛び / 2枚目バッジの跳ね / 30枚到達の光り / 絞り込みモーダルの膨らみ
- [x] G 図鑑:ページめくり / 右ページのホバー傾き / 並び替えのスライド
- [x] H ショップ・アカウント:購入の飛び / 名札の跳ね / 買えない品の無反応
- [x] I 一覧系:段差フェードイン / 削除の縮小
- [ ] 実機で手触りを確認して尺・振れ幅を詰める(§1へ移す)

## 4. コンテンツの残り

### カードセット(所有の土台は実装済み)

仕様は GameDesign.md 8章・15章・21章、実装設計は Architecture.md 10.8.1節。
`CardData.set_id` / `CardSetLibrary` / `players/{uid}.owned_card_sets` /
`AccountService.unlock_card_set()` と、デッキ編集のロック表示・図鑑のシルエット表示は実装済み。

- [x] ~~最初の購入可能なカードセットを1つ設計する~~ → 「五砂の刻」(コンボ系5枚・500砂金)を
      `combo_five` として実装済み。詳細は `docs/Hourglasses.md`「カードセット: 五砂の刻」
- [ ] ショップ画面のカードセット品目の見た目を確認する(実機確認。§1のテーブルへ追加済み)
- [ ] デッキ編集でロックされたカードを選んだときの「ショップへの導線」。
      いまは行き先が無い(これまでソロモードセットはショップに並ばず確認できなかったが、
      「五砂の刻」が最初の実例になる)

### コンボ系5枚「五砂の刻」の仕上げ

- [x] ~~紋章(emblem)が5枚とも未着手~~ → ミドル・フェイズ・キー・サイクル・クレストの
      5枚とも `assets/hourglasses/emblems/{id}.png` を作成し、各`.tres`へ反映済み
- [x] ~~Discordへの告知~~ → 「次のアップデートで追加される予定」という予告として
      #お知らせへ投稿済み(`tools/discord/drafts/cards/combo-five-1.md`)。**次回
      `ビルドして`(export_web.sh)を実行して実際にWeb版へ反映するまでが対になる作業**

### ソロモード限定カード3枚の仕上げ

- [x] ~~紋章(emblem)が3枚とも未着手~~ → チャイム・ウォード・揺さぶりの一手の3枚とも
      `assets/hourglasses/emblems/{id}.png` を作成し、各`.tres`へ反映済み
- [x] ~~3枚ぶんのDiscord告知~~ → 追加からだいぶ時間が経っているため見送り(ユーザー判断)

### ソロモードの積み残し

- [ ] 勝利条件の監視(`SURVIVE_TURNS` / `DESTROY_ALL_ENEMY_UNITS`)に自動テストが無い。
      `CardMatchScreen`(Control)を経由するため、既存のテストと同じく `MatchState` 単体では
      押さえられない。該当するステージを作ったときに実機で確認する

### リーサルパズル

- [ ] **問題を増やす。**いまの10問は守護・貫通・速落・砂術と反転をひととおり触るだけで、
      「相打ちで自分が先に砕ける」「毒砂」「連撃」を扱う問題がまだ無い

### 反転権の案内

- [ ] デイリーミッション「反転を8回行う」(23章)で反転権の使用を数えるかどうかが未決定。
      いまは `unit_flipped` だけを見ており数えていない。2つの反転手段を同じ指標で数えるべきか
      ユーザーへ確認してから決める

### イラスト

- [ ] 固有の絵を持つカードは1種も無く、全種がサンドの色違い。差し替えたいカードだけ
      個別の絵を用意できる状態は維持してある(Architecture.md 4.1節)。
      **色相はもう刻めない**ため、さらに種類を増やすときは枠の形か装飾を変える手段が要る
      (GameDesign.md 9章)

---

## 4.5. ランクマッチ・掲示板・公式大会(2026-09-15・仕様確定/未着手)

仕様はGameDesign.md 28章、実装設計はArchitecture.md 10.16節。**着手順はランクマッチ→
掲示板→公式大会と決めてある。**掲示板・公式大会はまだ仕様を書いていないため、
ランクマッチが一段落してから改めてGameDesign.mdへの追記案を出す。

### ランクマッチ(実装タスク・2026-09-15実装)

- [x] ~~`RankRules`(段位表・星の必要数・レートの増減表)~~ → `scripts/logic/rank_rules.gd`。
      通信を持たない純粋ロジックとして実装し、`tools/tests/rank_tests.gd` で検証済み
- [x] ~~`players/{uid}` へ `rank_season` / `rank_tier` / `rank_stars` / `rank_rating` /
      `rank_peak_tier` / `rank_reward_claimed_season` を追加~~ → `AccountService`の
      `_empty_profile()`とgetterに追加
- [x] ~~`RankProgress.ensure_current_season()`(遅延シーズンリセット・月末報酬の付与)~~
- [x] ~~`RankProgress.apply_result()`(星取り制の昇格 / プラチナのレート変動 /
      10手未満の対局を対象外にする不正対策)~~
- [x] ~~`RankedMatchmakingQueue` + `CardRankedMatchScreen`(`CardRandomMatchScreen`の複製に近い)~~
- [x] ~~`MatchKind.RANKED` を追加し、`CardMatchOutcome` から `RankProgress.apply_result()` を呼ぶ~~
- [x] ~~`CardRankScreen`(自分の段位表示 + ランキング一覧)。`たたかう`タブへの入口追加~~ →
      「だれかと」枠にランクマッチのタイルを追加(ランダムマッチ/ルームマッチと3枚並び)、
      ランクマッチ画面のヘッダーから「ランキング」で`CardRankScreen`を開く
- [x] ~~ランキングをプラチナ限定からブロンズ〜プラチナ全員へ広げる~~(2026-09-16、
      ユーザー判断「プラチナだけじゃなくて進行度でもランクに残ったら嬉しそう」)。
      `RankRules.progress_score()`で帯・階級・★・レートを1本の合成スコアへ束ね、
      `players/{uid}.rank_progress_score`として持つ。クエリは`rank_season`の単一等価
      フィルタ+`rank_progress_score`降順の`orderBy`(以前の`rank_tier == "platinum"`との
      複合フィルタより単純化)
- [x] ~~月末報酬の額を決める~~(2026-09-15、ユーザー判断で確定)。
      `RankProgress.SEASON_REWARDS`:ブロンズ100/シルバー300/ゴールド800/プラチナ3000砂金
- [x] ~~unityroomランキング連携の技術調査~~(2026-09-15実装。Architecture.md 10.16節)。
      →一方向のスコア送信のみ可能と判明。`UnityroomRankingClient`を実装し、プラチナの
      レートが動くたびに送るようにした。**unityroom側のゲーム管理画面で、対応する
      ボードの記録方式を「常に記録」に設定する作業がユーザー側に残っている**
      (このプロジェクトのコードからはボードの作成・設定変更ができない)。既定の
      ボードNo(1)が実際のボード番号と合っているかも確認すること
- [x] ~~月初の表彰演出の見せ方を決める~~(2026-09-15、ユーザー判断で「モーダル表彰式」を
      採用。`CardSeasonCeremonyPanel`を実装し、`RankProgress.ensure_current_season()`が
      シーズンの切り替わりを検知したときだけ、前シーズンの到達段位と月末報酬を添えて
      ポップイン表示する。初めてランクマッチへ触れるプレイヤー(前のシーズンが存在しない)
      には出さない
- [ ] 実装後、CPU自己対戦ではなく**実際にランクマッチを何局か回して**、レートの動きが
      不自然でないか確認する(§1の実機確認テーブルへ追加済み)
- [ ] マッチング精度の改善(段位が近い相手を優先する)は当面見送り、早い者勝ちのまま
      (Architecture.md 10.16節「次のステップ」)
- [x] ~~連勝ボーナス(ゴールド以下で3連勝以上のとき★+1)~~(2026-09-16、ユーザー指示で追加)。
      `RankRules.star_gain()`・`players/{uid}.rank_win_streak`を実装。`CardRankScreen`の
      自分の状態欄に「現在n連勝中」を表示(ボーナスが乗っているかも添える)

### 掲示板(ラボ)(実装タスク・2026-09-15仕様確定)

仕様はGameDesign.md 29章、実装設計はArchitecture.md 10.17節。

- [x] ~~`players/{uid}` へ `owned_titles`(Array[String])を追加。「発案者」称号を
      `UserProfileLibrary` へ登録する~~ → 「発案者」「箱庭王」(30章の優勝賞品も見込んで
      先に登録)の2つを追加。`get_available_title_ids()` は初期の2つ(novice/none)だけを
      返すよう変更し、`AccountService.owned_titles()` と合わせて `AccountScreen` が
      一覧を組む(所有していないものは選ばせない。アイコンと同じ流儀)
- [x] ~~`LabModeration.quick_check()`~~ → `scripts/logic/lab_moderation.gd`
- [x] ~~`LabProposalService`(`submit()` / `list_approved()` / `vote()`)~~ →
      `scripts/net/lab_proposal_service.gd`。`FirestoreClient.query_two_fields_equal()`
      を新設し(既存の等価フィルタ1本のクエリと同じ流儀で、複合フィルタでも
      orderByを重ねなければ複合インデックスを要求しないことを利用)、
      `status == "approved" and month == 今月` の絞り込みに使う
- [x] ~~`AccountService.is_registered()`~~ → 投稿・投票の両方の入口で見る。
      未登録時は投稿ボタンを押すと案内を出し、投票・投稿の`Service`側でも
      二重に拒否する(ボタンの無効化だけに頼らない)
- [x] ~~`LabTab` + `CardLabScreen` + `LabSubmitPanel` + `LabProposalCard`~~ →
      ホーム画面へ5つ目のタブ「つくる」を追加(`HomeScreen`の並び・下部タブ・
      背景切り替えを4タブから5タブへ拡張)。一覧は横2列のグリッドで、
      ヘッダー右に「投稿する」と「今月/過去ログ」の切り替えを置く
- [x] ~~NGワードの禁止語リストを用意する~~ → 最低限の語数で `LabModeration.BANNED_WORDS`
      へ置いた。運用しながら増やす
- [x] ~~`functions/lab_admin.js`~~ → `list_pending` / `list_current_month` / `approve` /
      `reject`(返金つき、Firestoreトランザクション) / `set_result`(採用時は
      称号「発案者」付与つき) / `grant_tournament_prize`(30章の優勝賞品を先取りで実装。
      称号「発案者」「箱庭王」を同時付与)。`functions/index.js` へ
      `exports.labAdmin`(`X-Admin-Secret`ヘッダーでの認証)を追加
- [x] ~~`tools/lab_admin/index.html`~~ → エンドポイント・シークレットを`localStorage`へ
      保存するバニラHTML+JS。審査待ち一覧(承認/却下)と今月の得票ランキング
      (採用/不採用の確定)の1画面構成
- [x] ~~`firestore.rules` へ `lab_proposals` / `lab_proposals/{id}/votes/{uid}` を追加~~
- [x] ~~検証(`tools/tests/lab_tests.gd`)~~ → NGワード判定・月キーの形式・
      称号一覧の絞り込み・`is_registered()`に加え、`FakeFirestoreClient`を使って
      「投稿→残高が減り`pending`のドキュメントができる」「投票が1回だけ通り
      2回目は拒否される」までを実通信なしで確認済み(`run_tests.gd`へ登録し、
      全体のヘッドレステストが通ることを確認済み)
- [x] ~~`LAB_ADMIN_SECRET`の設定と`labAdmin`のデプロイ~~(2026-09-17)。
      `firestore.rules`もFirebaseコンソールへ反映済み。**関数の作成時にIAMの
      「未認証呼び出しを許可」の設定が失敗する既知の不具合を踏んだ**(1回目の
      デプロイは成功したが403 Forbiddenが返り続けた)。`gcloud run
      services add-iam-policy-binding labadmin --region=us-central1
      --member=allUsers --role=roles/run.invoker` を1回実行して解消した。
      **併せて`listPending()`が`status`の等価フィルタと`created_at`の`orderBy`を
      重ねており複合インデックスを要求していたことも発覚**(6章のクエリ方針に
      反していた)。Firestore側のorderByをやめ、JS側でソートする形へ直して
      再デプロイし、`curl`での動作確認(`list_pending` / `list_current_month`)
      まで完了した
- [ ] `tools/lab_admin/index.html`のエンドポイント欄へ
      `https://labadmin-ske5k5hcaa-uc.a.run.app` を、シークレット欄へ発行した
      `LAB_ADMIN_SECRET`を入力し、実際にブラウザから承認・却下・採用確定の
      一連の流れを1人で通しで確認する(§1の実機確認テーブルへ追加済み)

### 公式大会「箱庭杯」(実装タスク・2026-09-15仕様確定)

仕様はGameDesign.md 30章、実装設計はArchitecture.md 10.18節。**大会名・称号名は
仮称のまま**(30章に明記のとおり、後で世界観に合わせた名前へ差し替えてよい)。

- [ ] `lab_proposals` へ `source`(`"vote"` / `"tournament"`)フィールドを追加
- [ ] `functions/lab_admin.js` へ `grant_tournament_prize()` を追加(称号2件
      「発案者」「箱庭王」を同時付与)。称号「箱庭王」を `UserProfileLibrary` へ登録
- [ ] `tools/lab_admin/index.html` へ大会優勝者登録フォームを追加
- [ ] `CardLabScreen` / `LabProposalCard` に「大会優勝作」の印表示(`source == "tournament"`)
- [ ] 初回開催に向けて、Discordでの告知・予選日程の案内文を用意する(運用面。
      コードの変更ではない)

---

## 5. バランスの測り直し(まとめて1回で行う)

**個別に測り直すと基準そのものが動いて無駄になる。**次にプールを触る回に、下記をまとめて行う。

- [ ] **カード1枚ごとの勝率(`docs/Hourglasses.md`)は、デッキ20枚・初期HP30時代の測定のまま。**
      枚数もHPも変わっているため前提が崩れている
- [ ] **35枚(50種化の27種 + マナカーブ調整の8種)と砂術12種は各400戦(誤差±5pt)でしか
      測っていない。**800戦で測り直す
- [ ] **先手勝率が54.8%、逆転率が38.6%**(目標45〜55% / 25〜35%)。**先に当たるのは逆転率**。
      手番の補正は引き続き足さない(効き幅が大きく、重ねると必ず行き過ぎる。GameDesign.md 7章)
- [ ] **初期HP24化で先手勝率が57〜60%まで上振れすることは承知のうえで採用した。**
      手番の補正(コイン維持・ヒーローパワー案)を検討する回に、あわせて調整する
- [ ] **CPUは砂術を持て余す**(BalanceReport_v5.md 10.6節)。1手ごとの貪欲法で1手番の
      マナ配分を見ないため、実測は砂術を過小評価している可能性が高い。人のプレイテストと突き合わせる
- [ ] 弱い砂術6枚を強めるかどうか。**強めた案と実測値は測定済み**(同10.2節)。
      **砂塵嵐(58.0%)は、はみ出したまま出すと決めてある**(ユーザー判断。調整漏れではない)
- [ ] 記録が貯まったら `tools/analyze_matches.py` を回し、シミュレーション値と突き合わせる

---

## 6. 技術的負債

- [ ] **`scripts/ui/card_effect_preview.gd` が995行**で、gdlint の上限(1000行)まで5行しかない。
      **次に実演を1本足すと落ちる。**台本(`Script` enum ごとの `_stage()`)と図版の描画を
      分けるのが自然な線
- [ ] **`scripts/ui/card_match_screen.gd` が981行 / `scripts/ui/card_view.gd` が994行**(手触りの回で
      `CardViewPaint` へ描画を切り出したが、身構え・跳ね・取り消しの状態を足して元の行数へ戻った)。
      いずれも足す前に切り出す(`_screen` 参照を持つ `RefCounted` へ分ける既存の流儀)。
      `card_view.gd` は反転の段取り(`play_flip()` 一式)を `CardViewStrike` と同じ形で分けられる
- [ ] **`tools/tests/run_tests.gd` が876行の `v5_rules_tests.gd` と並んで上限に近い。**
      新しいテストは引き続き別ファイルへ置く
- [ ] **本文用フォントが1.66MB**で、pck 4.1MBの4割にあたる。デッキ名がユーザーの自由入力である以上
      サブセット化すると入力できない文字が出るため手を付けていない。起動時間をさらに詰める
      必要が出た時点で判断する。**2026-09-17、タイトル・見出し専用の明朝体(Zen Old Mincho)を
      別途追加した**が、こちらは使う文字列が固定(タイトルロゴ・共通ヘッダー・ホーム画面の
      見出し)なため`pyftsubset`で削り、pckへの寄与は約100KBに抑えてある
      (`UiFonts`、Architecture.md 4章)
- [ ] **Discord用の生成物が63MB**(カード画像7.6MB・実演GIF55MB)。次にカードを増やす回に、
      GIFの色数・尺を削るか、撮影する能力の種類を絞るかを判断する

---

## 7. ロードマップ

| # | フェーズ | 内容 |
|---|---|---|
| 42 | **レアリティ設定 & レジェンドカード** | 検証勝率による★0〜★2付与 ＋ ★3レジェンド(デッキ1枚制限・専用演出) |
| 43 | **クラスシステム & ヒーローパワー** | 砂時計の形状に基づく5クラス ＋ ヒロパ ＋ 新カード75枚 |
| 44 | **カードセットの販売** | ショップでの買い切り(ガチャ・錬金は不採用) |

#42〜#44 を1回でまとめて実現する案の検討メモが `docs/NextCardSet.md` にある
(**未確定**。採用が決まった項目から GameDesign.md への追記案として切り出す)。
